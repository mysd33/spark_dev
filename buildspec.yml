version: 0.2

phases:
  build:
    commands:
      - echo build
      - sbt clean package
  post_build:
    commands:
      - echo assembly
      - sbt assembly
artifacts:
  files:
    - '**/target/**/databricks_dev-assembly-*.jar'

